# Copia este archivo a .env y completa los valores

DATABASE_URL=
JWT_SECRET=
FRONTEND_URL=http://localhost:5173

# OAuth Google — Firebase Console > Authentication > Sign-in method > Google > "Web client ID"
GOOGLE_CLIENT_ID=

EMAIL_USER=
EMAIL_PASSWORD=
